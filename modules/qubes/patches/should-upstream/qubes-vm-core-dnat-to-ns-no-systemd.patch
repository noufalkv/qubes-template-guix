Make qubes-setup-dnat-to-ns portable to systemd-less VMs (no systemd-resolved,
no python-dbus, no systemctl).

qubes-setup-dnat-to-ns runs on every NetVM/ProxyVM (qubes-core integration tests
invoke it from configure_netvm).  On a native Guix template using the Shepherd it
fails in four ways, each of which makes the whole script exit non-zero so that
configure_netvm fails and networking tests error in setUp:

1. `import dbus` raises ModuleNotFoundError when python-dbus is not installed,
   before the existing /etc/resolv.conf fallback in get_dns_resolved() can run.
   Make the import optional and, when absent, read /etc/resolv.conf directly.

2. get_dns_resolved() unconditionally calls dbus.SystemBus(); short-circuit to
   the resolv.conf fallback when dbus is unavailable.

3. dbus.SystemBus() itself raises DBusException when python-dbus IS installed
   but no system bus is running (no /run/dbus/system_bus_socket) -- the common
   case on a systemd-less Shepherd VM that nonetheless ships python-dbus.  The
   original code only fell back on NoReply, re-raising FileNotFound /
   ServiceUnknown / NameHasNoOwner and crashing before the dnat-dns chain is
   installed, which silently breaks DNS for every downstream qube (their
   queries to the NetVM's nameserver alias get neither answered nor
   redirected).  Treat the bus-unavailable errors the same as NoReply and fall
   back to /etc/resolv.conf.

4. The firewall reload uses "systemctl --no-block try-reload-or-restart
   qubes-firewall.service"; systemctl is absent, raising FileNotFoundError.
   Fall back to SIGHUP'ing the running qubes-firewall daemon, which it already
   handles as a rules reload (see qubesagent/firewall.py main()).

systemd distributions are unaffected: dbus imports, systemd-resolved answers, and
systemctl is present, so all original paths run unchanged and the fallbacks are
never reached.

Upstream: QubesOS/qubes-core-agent-linux, network/qubes-setup-dnat-to-ns.
Status: should-upstream (portable; only adds fallbacks for systemd-less VMs).
diff --git a/network/qubes-setup-dnat-to-ns b/network/qubes-setup-dnat-to-ns
index caf95e4..b962585 100644
--- a/network/qubes-setup-dnat-to-ns
+++ b/network/qubes-setup-dnat-to-ns
@@ -25,7 +25,13 @@ from itertools import cycle
 import subprocess
 import sys
 
-import dbus
+try:
+    import dbus
+except ImportError:
+    # systemd-resolved (queried over D-Bus) is unavailable on systemd-less VMs
+    # such as a native Guix template using the Shepherd, and python-dbus may not
+    # be installed.  Fall back to /etc/resolv.conf in get_dns_resolved() below.
+    dbus = None
 import qubesdb
 from ipaddress import IPv4Address
 import os
@@ -48,10 +54,24 @@ def get_dns_resolv_conf():
     return nameservers
 
 def get_dns_resolved():
+    if dbus is None:
+        # No python-dbus / no systemd-resolved: use /etc/resolv.conf directly.
+        return get_dns_resolv_conf()
     try:
         bus = dbus.SystemBus()
     except dbus.exceptions.DBusException as s:
-        if s.get_dbus_name() == 'org.freedesktop.DBus.Error.NoReply':
+        # python-dbus is installed but the system bus is not reachable: no bus
+        # socket (FileNotFound) on a systemd-less Shepherd VM, or the bus is up
+        # but systemd-resolved is absent (ServiceUnknown / NameHasNoOwner).  In
+        # every "bus unavailable" case fall back to /etc/resolv.conf instead of
+        # crashing, which would otherwise skip installing the dnat-dns chain and
+        # break DNS for all downstream qubes.
+        if s.get_dbus_name() in (
+            'org.freedesktop.DBus.Error.NoReply',
+            'org.freedesktop.DBus.Error.FileNotFound',
+            'org.freedesktop.DBus.Error.ServiceUnknown',
+            'org.freedesktop.DBus.Error.NameHasNoOwner',
+        ):
             return get_dns_resolv_conf()
         raise
     try:
@@ -142,11 +163,18 @@ def install_firewall_rules(dns):
         sys.exit(100)
 
     if os.path.exists('/run/qubes-service/qubes-firewall'):
-        subprocess.call(["systemctl",
-                         "--no-block",
-                         "try-reload-or-restart",
-                         "qubes-firewall.service"
-        ])
+        try:
+            subprocess.call(["systemctl",
+                             "--no-block",
+                             "try-reload-or-restart",
+                             "qubes-firewall.service"
+            ])
+        except FileNotFoundError:
+            # systemd-less VM (e.g. a native Guix template using the
+            # Shepherd): systemctl is absent, so reload the running
+            # qubes-firewall daemon directly via SIGHUP, which it handles as a
+            # rules reload (see qubesagent/firewall.py main()).
+            subprocess.call(["pkill", "-HUP", "-f", "/qubes-firewall"])
 
     os.execvp("nft", ("nft", "--", "\n".join(preamble + rules)))
 
