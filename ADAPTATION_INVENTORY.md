# Adaptation Inventory

This file lists the Guix-specific adaptations that Qubes reviewers are most
likely to inspect.  It is not a substitute for reading the code; it is a map of
where behavior intentionally diverges from stock Qubes VM agent packaging.

## Package Build Adaptations

| Area | Code location | Adaptation | Rationale | Current validation |
| --- | --- | --- | --- | --- |
| Xen vchan runtime subset | `xen-vchan-libs` package | Copy only the Xen shared libraries needed by VM-side vchan/qrexec components and rewrite copied-library RPATHs to that reduced output. | The full Guix Xen package also retains hypervisor tools, QEMU, firmware, and OVMF that are not part of a Qubes TemplateVM guest runtime.  The template only needs the VM-side vchan library closure, while dom0 supplies the hypervisor and VM kernel. | Rootfs builds, image inspections, and nested-dom0 qrexec/QubesDB smoke exercise the resulting vchan/qrexec runtime; final publication reruns remain required. |
| Package-time test policy | Qubes VM packages with `#:tests? #f` | Disable package-time tests only where the selected upstream subdirectory has no test target or where upstream tests require a live Qubes/Xen/systemd/GUI environment. | A Guix package build container does not provide QubesDB, qrexec, Xen devices, systemd, or a Qubes GUI session.  The reviewable test story should use artifact and runtime gates instead of text-only substitutes. | `make check` covers the native rootfs pinned-channel policy plus Builder/RPM contracts, `make guix-system-contract-check` covers Guix system-record defaults, rootfs activation checks cover generated guest state, and nested-dom0/openQA gates cover Qubes runtime behavior; final publication reruns remain required. |
| VM-side component package split | `qubes-libvchan-xen`, `qubes-linux-utils-qrexec`, `qubes-vm-utils`, and `qubesdb-vm` build/install phases | Build selected VM-side upstream targets and install them with explicit `PREFIX`, `LIBDIR`, `BINDIR`, include, and Python `site-packages` paths: vchan, `linux-utils/qrexec-lib`, `linux-utils/qmemman`, and QubesDB daemon/client/bindings. | Qubes upstream repositories are normally assembled by RPM/Deb packaging specs that also carry distribution service files and host-side policy.  Native Guix packages need small, declarative guest-runtime outputs without importing distro package scripts as hidden policy. | Remote rootfs builds exercise these derivations; `make guix-system-contract-check` asserts the meminfo-writer service contract, and nested-dom0/openQA smoke covers QubesDB, qrexec, and memory-ballooning behavior for the recorded artifacts.  Final publication reruns remain required. |
| QubesDB daemon supervision | `qubesdb-vm`, phase `run-vm-daemon-in-foreground` | Prevent `db-daemon` from forking when built without systemd. | Shepherd supervises the child it starts; daemonizing hides the real daemon and can leave duplicate QubesDB daemons racing for the same vchan. | Guix module load, rootfs build/inspection snapshot, and nested-dom0 TemplateVM/AppVM QubesDB smoke evidence exist; the final publication branch or release object still needs fresh validation. |
| qrexec login shell paths | `qubes-vm-qrexec`, phase `support-guix-login-shell-paths` | Increase qrexec-agent environment buffer from 64 bytes to `PATH_MAX`. | Guix login shells live under `/gnu/store/...`, which can exceed the upstream short buffer. | Guix module load, rootfs activation, and nested-dom0/openQA qrexec smoke cover the recorded artifacts; final publication reruns remain required. |
| qrexec PAM selection | `qubes-vm-qrexec`, `build` and `install` phases | Pass upstream `HAVE_PAM_APPL=1` instead of patching PAM detection. | The upstream Makefile probes `/usr/include`; Guix inputs are not exposed there during the build. | Build smoke on a remote Guix builder and activation tests check qrexec PAM materialization. |
| qrexec wait service | `qubes-vm-qrexec`, `install` phase | Replace `qubes.WaitForSession` with a Shepherd-compatible check for qrexec fork-server readiness. | Upstream waits for systemd user-session state; native Guix uses Shepherd. | Activation tests cover the RPC wrapper, and nested-dom0/openQA smoke covers `qubes.WaitForSession`; the final publication branch or release object still needs fresh validation. |
| qrexec Python path and FHS layout | `qubes-vm-qrexec`, `install` phase Python block | Move `/usr/*` payloads into profile-visible `/bin`, `/lib`, `/share`, copy qrexec Python modules, and patch generated Python entry points. | Qubes installs to FHS paths, while Guix profiles expose package outputs without merging `/usr`. | Rootfs inspection, activation evidence, and nested-dom0 TemplateVM/AppVM qrexec smoke exist; the final publication branch or release object still needs fresh validation. |
| `/etc/skel` home initialization | `qubes-vm-core`, phase `dereference-guix-skel-in-home-init` | Resolve `/etc/skel` before `cp -a -T`. | Guix exposes `/etc/skel` as a generated store symlink; preserving the symlink would make new user homes point at immutable store state. | Activation tests cover `/etc/skel` materialization. |
| Core agent version propagation | `qubes-vm-core`, build phase | Derive package metadata and upstream `VERSION=` arguments from `%qubes-source-components`. | Avoid silent drift between pinned Qubes tags and package/build metadata. | Guix module-load checks assert package object versions against the pinned table under real Guix; release builds still require full rootfs/RPM/runtime validation. |
| Core agent packaging payloads | `qubes-vm-core`, `install` phase | Install upstream `qubes-rpc`, `network`, `misc`, `app-menu`, and `filesystem` payloads in addition to `install-corevm`. | Qubes RPM/Deb packaging installs some VM payloads outside the upstream `install-corevm` target.  Native Guix packaging still needs the network setup helpers, feature-reporting hooks, appmenu sync helpers, filesystem snippets, and `/usr/share/qubes/marker-vm` expected by Qubes post-install and Template Manager flows. | Rootfs inspection, RPM-mode openQA postinstall checks, network-uplink service wiring, appmenu smoke, and qvm-template lifecycle evidence cover these installed payloads for the recorded artifacts; final publication reruns remain required. |
| Core agent post-install hooks | `qubes-vm-core`, `install` phase Python block | Add profile-visible `PATH`, force `qubes.PostInstall` as root, add Guix updates proxy forwarder, preserve supported-service feature reporting without systemd, add Python paths, and install `qubes-firewall`/`qubes-vmexec` wrappers. | Qubes post-install hooks and feature reporting assume FHS paths and systemd. Native Guix needs the same Qubes-visible behavior through Shepherd/profile paths. The updates proxy wrapper uses the current Qubes `qrexec-client-vm --use-stdin-socket '' qubes.UpdatesProxy` form because the Shepherd service hands each TCP connection to `socat EXEC:`, which provides the child a stream socket on standard input/output. | Activation tests cover `qubes.PostInstall`; nested-dom0/openQA evidence covers postinstall diagnostics and update-proxy service state for the recorded artifacts; final signed release validation and real Guix update downloads remain open. |
| GUI agent generated configure scripts | `qubes-vm-gui`, phase `patch-generated-configure-invocation` | Invoke generated `configure` scripts with `CONFIG_SHELL` and avoid the top-level generated configure path that conflicts with Guix shebang patching. | Generated autotools files can retain shell assumptions that fail after Guix patches shebangs. | Remote rootfs build evidence and RPM-mode openQA GUI/appmenu smoke cover the recorded artifacts; final publication reruns and broader GUI usability checks remain required. |
| GUI agent build subset | `qubes-vm-gui`, `build` phase | Build the GUI agent, runuser helper, common Xorg library, and Qubes Xorg input/video drivers, while leaving audio-specific PulseAudio/PipeWire module targets out of the first-review package. | The current upstreamability scope is TemplateVM/AppVM qrexec, QubesDB, X11 GUI/appmenus, storage, swap, memory ballooning, and updates-proxy behavior.  Audio/PipeWire coverage should be reviewed and tested as a separate runtime gate rather than hidden inside the basic GUI package build. | Nested-dom0/openQA smoke covers basic GUI and appmenu behavior; `SECURITY.md` and `REVIEW_NOTES.md` still list audio/PipeWire usability checks as broader unpassed gates. |
| GUI startup under Guix | `qubes-vm-gui`, `install` phase | Install profile hooks, adjust `qubes-run-xorg`, provide Xorg module path, run root-owned xinit/Xorg while keeping the session command under the default user, and make the temporary root-owned Xorg/user-session split explicit with `-ac`. | The upstream non-GuiVM path expects distro Xorg/logind integration; the Guix image uses profile paths and does not provide that wrapper stack or a distro xauth handoff for a root-owned Xorg server to authorize the default user's session client.  The `-ac` flag is therefore review-sensitive compatibility state, not something to hide as ordinary GUI setup. | Nested-dom0/openQA smoke covers basic GUI and appmenu behavior; the final publication branch or release object still needs fresh validation and broader GUI/security testing, including whether this can be replaced by a narrower xauth/logind-style handoff. |
| GUI FHS normalization | `qubes-vm-gui`, `install` phase | Move `/usr/bin`, `/usr/lib`, `/usr/share`, `/usr/include` into profile-visible output paths and repair Qubes RPC symlinks. | Qubes GUI agent installs into distribution FHS paths; Guix activation links `/usr` compatibility paths back to the system profile. | Rootfs inspection and nested-dom0/openQA appmenu smoke evidence exist; the final publication branch or release object still needs fresh validation. |
| Xorg driver runpaths | `qubes-vm-gui`, phase `set-xorg-driver-runpath` | Add runtime library paths to built Xorg drivers. | Guix does not rely on a global dynamic linker path for package outputs. | Rootfs inspection and basic GUI smoke evidence exist; the final publication branch or release object still needs fresh validation. |

## Image Build and Reconfiguration Adaptations

| Area | Code location | Adaptation | Rationale | Current validation |
| --- | --- | --- | --- | --- |
| Default root image size | `scripts/build-native-rootfs.sh`, `scripts/run-native-guix-template-test-dom0.sh`, and openQA `GUIX_ROOT_SIZE` defaults | Use a 20G default root image, matching the current Qubes Builder v2 `template-root-size` default used by standard Fedora/Debian template entries unless a release config overrides it, instead of shrinking the release image default to the Guix closure size. | Keeps TemplateVM volume sizing aligned with Qubes template expectations and avoids making Guix a special small-root case that reviewers and users have to account for separately. | `VALIDATION.md` records normal and minimal 20G root image builds, Builder-shaped root image builds, and RPM-mode openQA evidence.  The local RPM contract tests intentionally use tiny generated images only for fast package-layout checks, not as release-size evidence. |
| Installed reconfiguration inputs | `scripts/build-native-rootfs.sh`, `config/channels.scm`, installed `/etc/config.scm`, and installed `/etc/guix/channels.scm` | Install a self-contained Guix system configuration containing the local Qubes package/service modules and install the pinned channel file into the template.  The rootfs builder uses `config/channels.scm` by default and fails if no pinned channel file is available, unless a maintainer explicitly sets `GUIX_BRANCH` for a developer-only build. | `guix system reconfigure /etc/config.scm` should not depend on a builder checkout or site module path after installation.  Release-quality image builds should start from a recorded Guix channel, while the Qubes central-updater RFC path uses `guix time-machine --branch=master -- system reconfigure /etc/config.scm` so vmupdate updates the Guix System generation without treating root's Guix checkout/profile as package-manager state. | Rootfs inspection and activation checks cover installed config materialization; `tests/build-native-rootfs-policy-check.sh` covers the missing-channel failure before image/mount work; remote evidence records `guix time-machine -C config/channels.scm -- describe`; final update/reconfigure release evidence from a publication branch or release object remains required. |
| Generated `/sbin/init` entrypoint | `scripts/build-native-rootfs.sh` | Write a small `/sbin/init` wrapper that uses the installed Guix system profile shell, prepares `/proc`, `/sys`, `/dev`, `/run/qubes`, `/run/qubes-service`, and `/var/run` compatibility paths, exports `GUIX_NEW_SYSTEM=/var/guix/profiles/system`, and then executes the Guix system boot program. | Qubes dom0 boots the template with its own kernel and expects `/sbin/init` in the guest root.  `guix system init --no-bootloader` installs the Guix system profile and boot program, but the Qubes boot path still needs a stable guest entrypoint and early runtime directories before Shepherd starts Qubes services. | `scripts/inspect-native-rootfs.sh` checks the wrapper is executable, uses the Guix profile shell, mounts `/proc` before Guix boot, and exports `GUIX_NEW_SYSTEM`.  Nested-dom0 and openQA boot evidence exercises the wrapper in actual TemplateVM/AppVM starts; final publication reruns remain required. |
| Manual dom0 root-image import helper | `scripts/import-native-rootfs-dom0.sh` | Create a fresh TemplateVM, set PVH and the selected Qubes kernel, resize the root volume to the Qubes-sized default, and import a native root image with `qvm-volume import`.  If Qubes file-pool storage refuses to shrink the just-created root volume, resolve the file-pool backing path and truncate it before import. | This helper is for disposable nested-dom0/root-image smoke testing before the Template Manager RPM path is used.  The direct truncate fallback is limited to a newly created, never-started TemplateVM owned by the script; it refuses to operate when the target VM already exists. | Nested-dom0 root-image smoke evidence and later RPM-mode openQA evidence cover the resulting TemplateVM behavior.  The release path remains `qvm-template` RPM install; direct root-image import is test infrastructure, not the publication mechanism. |
| Template Manager RPM payload | `scripts/package-native-template-rpm.sh` and `scripts/builder-v2-template-adapter.sh build-rpm` | Package the root image as split `root.img.part.NN` tar payloads, write `template.conf` with PVH/qrexec/GUI metadata, generate normal/minimal appmenu allowlists, include appmenu directories and `clean-volatile.img.tar`, mark `private.img` and `volatile.img` as RPM ghosts, and make `%pre` reject direct `rpm`/`dnf` installation. | Qubes templates are installed through `qvm-template`, not by ordinary package-manager installation.  The RPM should expose the Template Manager payload and metadata contract while leaving private and volatile images managed by Qubes. | `tests/rpm-layout-check.sh` and `tests/builder-rpm-contract-check.sh` build, extract, and reassemble generated RPM payloads for both variants; nested-dom0 lifecycle evidence covers `qvm-template` install, reinstall, remove, upgrade, and downgrade; final publication reruns remain required. |

## System Definition Adaptations

| Area | Code location | Adaptation | Rationale | Current validation |
| --- | --- | --- | --- | --- |
| Dom0-provided kernel | `qubes-dom0-kernel-bootloader`, system `bootloader`, `kernel`, `initrd-modules`, and `kernel-arguments` | Use a no-op bootloader record and a placeholder kernel package while building root images with `--no-bootloader`; keep an empty guest initrd module list and Qubes-style `console=hvc0`/`panic=1` kernel arguments; the generated bootloader config still references the Guix boot entry so `guix system init` keeps the full system closure reachable. | Qubes dom0 supplies the VM kernel; a native TemplateVM image should not pull in or install a guest bootloader stack, but the Guix image build still needs a complete system closure copied into the target root and the booted VM should use the expected Qubes console/panic behavior. | Rootfs builds and inspections pass; nested-dom0/openQA boots use the dom0-provided kernel path. |
| Lean Guix base services | `%qubes-base-services-with-lean-udev`, `%qubes-omitted-base-service-types`, `%qubes-loopback-networking-services`, and system `services` | Keep Guix base services needed by the image; omit `agetty`, `console-fonts`, `etc-bashrc-d`, `login`, `log-cleanup`, `log-rotation`, `mingetty`, `nscd`, `shepherd-timer`, `shepherd-transient`, `static-networking`, `sysctl`, and `virtual-terminal`; keep `guix-daemon` usable for ordinary AppVM networking instead of forcing it through an inactive localhost update proxy; keep udev without bundled rules; provide loopback-only static networking; and add DBus explicitly for GUI/session tooling. | Qubes provides console, kernel, device, and network lifecycle through VM agents; Guix services should not race those assumptions or install independent console/login/network/sysctl policy for a TemplateVM.  The Qubes updates-proxy forwarder is still present for update-client VMs, while update operations that must use it supply the proxy environment explicitly. | Rootfs activation, Guix module-load, nested-dom0/openQA smoke, and generated proxy configuration evidence exist for the current TemplateVM/AppVM scope; NetVM/ProxyVM behavior and real Internet Guix update downloads remain out of first-review proof. |
| Default Qubes user and privilege policy | system `users`, `groups`, `privileged-programs`, and `sudoers-file` | Provide the standard `user` account in `wheel` plus Qubes-related groups, keep Guix default setuid programs, add Qubes' `qfile-unpacker` through Guix privileged-program handling, and enable Qubes-style passwordless sudo. | Matches standard Qubes template defaults instead of inventing a Guix-specific login or sudo policy; Qubes file-copy RPC needs the unpacker to run with the intended privilege even though Guix store outputs themselves are not trusted as privileged executables. | `make guix-system-contract-check` instantiates both variants and asserts the standard `user` account, primary group `users`, supplementary `wheel`, `netdev`, `audio`, `video`, and `qubes` groups, preserved Guix default privileged programs plus Qubes `qfile-unpacker`, and passwordless `wheel`/`user` sudo; rootfs activation and nested-dom0/openQA smoke run as the expected default user with standard privileged helper behavior. |
| Normal/minimal package and appmenu split | `qubes-variant-packages`, `native/qubes-guix.scm`, `native/qubes-guix-minimal.scm`, `qubes-xterm-desktop-entry` | Keep both variants GUI-capable; provide `xfce4-terminal`, `thunar`, `mousepad`, and `evince` in the normal template; keep a small generated `xterm.desktop` package for the minimal template. | Mirrors the Qubes normal/minimal template expectation while making the normal template expose a basic discoverable desktop app surface instead of only Qubes generic wrappers; the minimal variant stays small and visible to appmenu discovery. | Root image inspection, RPM layout checks, nested-dom0 smoke, and RPM-mode openQA validate variant-specific command and desktop entries. |

## System Activation and Service Adaptations

| Area | Code location | Adaptation | Rationale | Current validation |
| --- | --- | --- | --- | --- |
| Qubes compatibility paths | `qubes-vm-compat-service-type` | Create `/usr/bin`, `/usr/sbin`, `/usr/share`, `/usr/lib/qubes`, `/etc/qubes-rpc`, `/run/qubes*`, `/var/run/qubes*`, `/bin`, and `/sbin/poweroff` compatibility links into the current system profile and runtime directories. | Qubes VM agents and RPC services use stable FHS paths; Guix package outputs live under immutable store paths exposed through profiles. | Activation tests cover key generated paths, and nested-dom0 TemplateVM/AppVM smoke evidence exists for both variants; the final publication branch or release object still needs fresh validation. |
| Mutable `/etc/fstab` | `qubes-vm-compat-service-type` | Materialize `/etc/fstab` if Guix exposes it as a store symlink. | Qubes `mount-dirs.sh` appends private-volume entries at runtime. | Activation tests and rootfs inspection. |
| Qubes PAM services | `qubes-qrexec-pam-service-type` | Provide PAM services through Guix `pam-root-service-type`. | Keeps PAM declarative instead of mutating `/etc/pam.d` in package phases. | Activation tests check qrexec PAM files. |
| ACPI shutdown | `qubes-acpi-shutdown-service-type` | Run `acpid` with Qubes event configuration under Shepherd. | Qubes `qvm-shutdown` relies on the guest handling ACPI power-button requests. | Nested-dom0 TemplateVM/AppVM smoke includes shutdown behavior; final signed release branch still needs fresh validation. |
| Xen and Qubes runtime setup | `one-shot-service`, `%xen-device-setup-command`, `%qubes-runtime-setup-command`, `qubes-sysinit-service-type` | Materialize Xen device nodes, Qubes runtime directories, service environment paths, and run upstream `qubes-sysinit.sh` before dependent services. | Qubes VM agents expect device nodes and runtime paths normally prepared by distro/systemd integration. | Rootfs activation, postinstall diagnostics, and nested-dom0/openQA smoke evidence exist; final signed release branch still needs fresh validation. |
| QubesDB and qrexec services | `qubes-db-service-type`, `qubes-qrexec-agent-service-type` | Run QubesDB and qrexec through Shepherd with Qubes runtime environment variables and device setup. | Native Guix uses Shepherd; Qubes agents still need Xen device permissions and Qubes runtime paths. | Nested-dom0 TemplateVM/AppVM qrexec and QubesDB smoke evidence exists for both variants; final release branch still needs fresh validation. |
| qrexec fork server | `qubes-vm-gui` package XDG autostart path and `qubes.WaitForSession` wrapper | Keep upstream's XDG autostart launcher as the owner of `qrexec-fork-server`, and make `qubes.WaitForSession` wait for that server's socket. | `qrexec-fork-server` daemonizes and belongs to the user GUI session readiness path, not root Shepherd supervision. | Nested-dom0/openQA smoke covers qrexec and session readiness; the final publication branch or release object still needs fresh validation. |
| GUI agent service | `qubes-gui-agent-service-type` | Start `qubes-gui` only when QubesDB says GUI is enabled, consume `/run/qubes-service-environment`, and preserve Qubes GUI options. | Qubes GUI startup state is communicated through QubesDB and generated service environment rather than Guix desktop session defaults. | Nested-dom0/openQA smoke covers basic GUI and appmenu behavior; broader GUI usability remains a release gate. |
| meminfo writer | `qubes-meminfo-writer-service-type` | Supervise Qubes `meminfo-writer` through Shepherd, honoring `/run/qubes-service/meminfo-writer` and standard threshold/delay defaults. | Matches Qubes guest-side memory ballooning plumbing without systemd. | `make guix-system-contract-check` asserts the service exists in both variants with the standard threshold, delay, and pidfile defaults.  Nested-dom0 AppVM smoke verifies the service flag, pidfile, live process, and `pgrep -x meminfo-writer`; `scripts/test-memory-balloon-dom0.sh` also passed with the AppVM growing from 400 MiB to 698 MiB under pressure. |
| Qubes-managed network uplink | `qubes-network-uplink-service-type` | Bring loopback up, wait for the Qubes-managed interface, and run upstream `setup-ip add`. | AppVM networking should use Qubes-provided network interface setup rather than independent Guix network policy. | Service is present in the TemplateVM/AppVM graph; first-review scope still excludes NetVM/ProxyVM ownership and needs final publication runtime validation. |
| updates proxy | `qubes-updates-proxy-forwarder-service-type`, Guix update-operation environment, and core agent wrapper | Forward update-operation traffic through Qubes `qubes.UpdatesProxy` without hiding ordinary Guix commands behind a global wrapper; do not implement a separate updater policy. | Keeps network/update behavior inside the Qubes RPC model. Shepherd provides the local TCP listener via `socat`, while the per-connection wrapper follows Qubes' current socket-activated `--use-stdin-socket` command to preserve bidirectional EOF handling. The Guix daemon itself is not forced through `127.0.0.1:8082`, because that listener is only active when Qubes marks the VM as an update client. | Controlled nested-dom0 smoke verifies forwarding to a temporary dom0 `qubes.UpdatesProxy` target, the 2026051602 nested-dom0 run verifies default `@type:TemplateVM -> @default target=sys-net` forwarding through stock Qubes policy, and jobs 31, 38, 41, and 44 verify a controlled `guix download` through a temporary `sys-net` stub target. Job 44 includes the no-direct-default-route guard. OpenQA job 49 passed RPM import and TemplateVM/AppVM smoke before the central raw-proxy preflight failed with `Request refused` and `updatevm` unset. Jobs 29 and 40 reached the real-network download verifier but dom0 refused `qubes.UpdatesProxy`; real Guix update-tooling downloads through an Internet update target remain a release gate. |
| private volume layout | `qubes-mount-dirs-service-type`, `qubes-bind-dirs-service-type`, system `swap-devices` | Use standard `/dev/xvdb -> /rw`, `/rw/home -> /home`, `/rw/usrlocal -> /usr/local`, Qubes bind-dirs, and `/dev/xvdc1` swap. | Matches the standard Qubes template storage model while expressing it declaratively in Guix. | `make guix-system-contract-check` asserts both variants declare `/dev/xvdc1` swap.  Nested-dom0 AppVM smoke verifies `/rw`, `/home`, `/usr/local`, home persistence, `/dev/xvdb`, `/dev/xvdc1`, and active `/proc/swaps` entry; final release branch still needs fresh validation. |
| Late Qubes setup | `qubes-misc-post-service-type` | Run upstream `misc-post.sh` as a best-effort late service and log failures without aborting Guix boot. | In systemd templates this late unit can fail while leaving the VM usable for qrexec and postinstall; Shepherd boot one-shot failures would otherwise be fatal. | Postinstall/openQA log scans catch known fatal failures; final signed release branch still needs fresh validation. |

## Review Notes

- The adaptations above should remain small and Guix-specific.  If an upstream
  Qubes release removes the need for one, drop the local adaptation rather than
  carrying it forward.
- Source-changing substitutions are intentionally named by phase.  If Qubes
  maintainers prefer patch files, the source-edit rows above are the conversion
  candidates.
- `VALIDATION.md` is the source of truth for which adaptations have live
  TemplateVM/AppVM evidence and which are still only build/rootfs/module-load
  evidence.
